# Prosey

CLI tool that downloads YouTube video transcripts for quick reading, skimming,
and searching — no watching required.

Accepts a YouTube URL or video ID, outputs plain text to stdout (or file), with
optional timestamps. Built with Bun + `youtube-transcript-plus`.

See [spec](spec-youtube-transcript-cli.md).
